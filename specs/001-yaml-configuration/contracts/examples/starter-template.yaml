# dottie.yaml - Generated by dottie init
# Documentation: https://github.com/ryandanthony/dottie
#
# This is a starter template. Uncomment and modify sections as needed.
# Required fields are filled in; optional fields are commented out.

profiles:
  default:
    # Dotfiles: Map source files in this repo to target locations
    dotfiles:
      - source: dotfiles/bashrc
        target: ~/.bashrc
      # - source: dotfiles/vimrc
      #   target: ~/.vimrc
      # - source: dotfiles/config/nvim
      #   target: ~/.config/nvim

    # Install: Define software to install (all sections optional)
    install:
      # APT packages (simple package names)
      apt:
        - git
        - curl
      #   - htop
      #   - tmux

      # GitHub releases (download binaries from releases)
      # github:
      #   - repo: junegunn/fzf           # Required: owner/repo
      #     asset: fzf-*-linux_amd64.tar.gz  # Required: glob pattern for asset
      #     binary: fzf                   # Required: binary name to extract
      #     # version: "0.45.0"           # Optional: specific version (default: latest)

      # Snap packages
      # snap:
      #   - name: code                    # Required: snap name
      #     classic: true                 # Optional: classic confinement (default: false)
      #   - name: spotify

      # Third-party APT repositories
      # apt-repo:
      #   - name: docker                  # Required: identifier
      #     key_url: https://download.docker.com/linux/ubuntu/gpg  # Required
      #     repo: "deb [arch=amd64] https://download.docker.com/linux/ubuntu jammy stable"  # Required
      #     packages:                     # Required: packages from this repo
      #       - docker-ce
      #       - docker-ce-cli

      # Custom scripts (must be within this repository)
      # scripts:
      #   - scripts/install-nvm.sh
      #   - scripts/setup-golang.sh

      # Fonts to download and install
      # fonts:
      #   - name: JetBrains Mono         # Required: display name
      #     url: https://github.com/JetBrains/JetBrainsMono/releases/download/v2.304/JetBrainsMono-2.304.zip  # Required

  # Additional profiles can extend others:
  # work:
  #   extends: default                  # Inherit from default profile
  #   install:
  #     apt:
  #       - awscli                      # Added to default's apt list
